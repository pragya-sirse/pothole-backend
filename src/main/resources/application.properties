# ============================================================
# SERVER CONFIG
# ============================================================
server.port=${PORT:8080}
spring.application.name=pothole-backend
server.forward-headers-strategy=framework

# ============================================================
# DATABASE ? TiDB Cloud
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/pothole-db?useSSL=true&requireSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&enabledTLSProtocols=TLSv1.2,TLSv1.3}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:3UqDGhtEGSVGzep.root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:your_tidb_password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-test-query=SELECT 1

# ============================================================
# JPA
# ============================================================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jackson.serialization.fail-on-empty-beans=false

# ============================================================
# JWT
# ============================================================
app.jwt.secret=${APP_JWT_SECRET:pothole_detection_mp_secret_key_2025_indore_bhopal_vidisha}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

# ============================================================
# EMAIL ? Gmail SMTP
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:nidhiqwerty2@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:jzjj byfb hsud wfnd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000

spring.mail.properties.mail.smtp.writetimeout=10000


# ============================================================
# ML SERVICE
# ============================================================
ml.service.url=${ML_SERVICE_URL:https://ml-flask-model-3jg4.onrender.com/detect}

# ============================================================
# FILE UPLOAD
# ============================================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ============================================================
# CORS
# ============================================================
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:*}

# ============================================================
# LOGGING
# ============================================================
logging.level.com.pothole=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN
logging.level.com.zaxxer.hikari=WARN
spring.jackson.serialization.write-dates-as-timestamps=false

cloudinary.cloud-name=${CLOUDINARY_NAME:doeq6b4ve}
cloudinary.api-key=${CLOUDINARY_KEY:455542683981244}
cloudinary.api-secret=${CLOUDINARY_SECRET:OSYMJC9iGLij9YrcLKQFBByw1Tg}

app.backend.url=https://pothole-backend-gbud.onrender.com

app.demo.authority.email=pragyasirse@gmail.com

# Performance ? Connection pool tuning

spring.datasource.hikari.keepalive-time=60000

# Reduce Hibernate startup time
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.defer-datasource-initialization=false

# Reduce logging overhead on Render
logging.level.root=WARN
logging.level.org.springframework.web=WARN


